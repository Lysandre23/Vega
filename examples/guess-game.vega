(var result (floor (* 100 rand)))
(var guess -1)
(while (!= guess result) (do
    (set guess (parse (ask "Enter you guess : ")))
    (if (> guess result) 
        (print "It's less")
        ()
    )
    (if (< guess result)
        (print "It's more")
        ()
    )
))
(print "Good guess !")